[project]
name = "workmate-testwork"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "argparse>=1.4.0",
    "loging>=0.0.1",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "sphinx>=9.1.0",
    "tabulate>=0.10.0",
]
